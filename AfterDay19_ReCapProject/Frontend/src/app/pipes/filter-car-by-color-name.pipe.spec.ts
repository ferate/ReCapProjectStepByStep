import { FilterCarByColorNamePipe } from './filter-car-by-color-name.pipe';

describe('FilterCarByColorNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCarByColorNamePipe();
    expect(pipe).toBeTruthy();
  });
});
